diff --git a/content/browser/renderer_host/render_process_host_impl.cc b/content/browser/renderer_host/render_process_host_impl.cc
index 687698d6ff258..0e0b0d7ea55dd 100644
--- a/content/browser/renderer_host/render_process_host_impl.cc
+++ b/content/browser/renderer_host/render_process_host_impl.cc
@@ -3443,6 +3443,7 @@ void RenderProcessHostImpl::PropagateBrowserCommandLineToRenderer(
     switches::kRendererWaitForJavaDebugger,
 #endif
 #if BUILDFLAG(IS_WIN)
+    sandbox::policy::switches::kDisableWin32kLockDown,
     switches::kDisableHighResTimer,
     switches::kEnableWin7WebRtcHWH264Decoding,
     switches::kTextContrast,
diff --git a/content/browser/service_host/utility_sandbox_delegate_win.cc b/content/browser/service_host/utility_sandbox_delegate_win.cc
index 9679599074584..544d920bd5241 100644
--- a/content/browser/service_host/utility_sandbox_delegate_win.cc
+++ b/content/browser/service_host/utility_sandbox_delegate_win.cc
@@ -8,6 +8,7 @@ class UtilitySandboxedProcessLauncherDelegate
 #include "content/public/common/content_switches.h"
 #include "content/public/common/sandboxed_process_launcher_delegate.h"
 #include "content/utility/sandbox_delegate_data.mojom.h"
+#include "sandbox/policy/switches.h"
 #include "sandbox/policy/features.h"
 #include "sandbox/policy/mojom/sandbox.mojom.h"
 #include "sandbox/policy/win/sandbox_win.h"
@@ -205,10 +205,12 @@ class UtilitySandboxedProcessLauncherDelegate
 
   if (sandbox_type_ == sandbox::mojom::Sandbox::kService ||
       sandbox_type_ == sandbox::mojom::Sandbox::kServiceWithJit) {
+    if (sandbox::policy::IsWin32kLockdownEnabled()) {
     auto result = sandbox::policy::SandboxWin::AddWin32kLockdownPolicy(config);
     if (result != sandbox::SBOX_ALL_OK) {
       return false;
     }
+    }
 
     config->SetFilterEnvironment(base::FeatureList::IsEnabled(
         sandbox::policy::features::kWinSboxFilterServiceEnvironment));
diff --git a/content/renderer/pepper/content_renderer_pepper_host_factory.cc b/content/renderer/pepper/content_renderer_pepper_host_factory.cc
index 9648fe8ee4ccc..dbcdc91c0ccc7 100644
--- a/content/renderer/pepper/content_renderer_pepper_host_factory.cc
+++ b/content/renderer/pepper/content_renderer_pepper_host_factory.cc
@@ -37,6 +37,7 @@
 #include "ppapi/proxy/ppapi_messages.h"
 #include "ppapi/proxy/serialized_structs.h"
 #include "ppapi/shared_impl/ppb_image_data_shared.h"
+#include "sandbox/policy/switches.h"
 #include "third_party/blink/public/platform/web_url.h"
 #include "third_party/blink/public/web/web_document.h"
 #include "third_party/blink/public/web/web_plugin_container.h"
@@ -125,11 +126,14 @@ ContentRendererPepperHostFactory::CreateResourceHost(
       ppapi::PPB_ImageData_Shared::ImageDataType image_type =
           ppapi::PPB_ImageData_Shared::PLATFORM;
 #if BUILDFLAG(IS_WIN)
-      // Win32K lockdown mitigations are enabled for Windows 8 and beyond.
-      // We use the SIMPLE image data type as the PLATFORM image data type
+      // If Win32K lockdown mitigations are enabled for Windows 8 and beyond
+      // we use the SIMPLE image data type as the PLATFORM image data type
       // calls GDI functions to create DIB sections etc which fail in Win32K
       // lockdown mode.
-      if (base::win::GetVersion() >= base::win::Version::WIN8)
+      // TODO(ananta)
+      // Look into whether this causes a loss of functionality. From cursory
+      // testing things seem to work well.
+      if (sandbox::policy::IsWin32kLockdownEnabled())
         image_type = ppapi::PPB_ImageData_Shared::SIMPLE;
 #endif
       scoped_refptr<PPB_ImageData_Impl> image_data(new PPB_ImageData_Impl(
diff --git a/content/renderer/pepper/resource_creation_impl.cc b/content/renderer/pepper/resource_creation_impl.cc
index 696feb4b63a7b..eecb529f3ff67 100644
--- a/content/renderer/pepper/resource_creation_impl.cc
+++ b/content/renderer/pepper/resource_creation_impl.cc
@@ -21,6 +21,7 @@
 #include "ppapi/shared_impl/ppb_image_data_shared.h"
 #include "ppapi/shared_impl/ppb_input_event_shared.h"
 #include "ppapi/shared_impl/var.h"
+#include "sandbox/policy/switches.h"
 
 #if BUILDFLAG(IS_WIN)
 #include "base/command_line.h"
@@ -148,7 +149,10 @@ PP_Resource ResourceCreationImpl::CreateImageData(PP_Instance instance,
   // we use the SIMPLE image data type as the PLATFORM image data type
   // calls GDI functions to create DIB sections etc which fail in Win32K
   // lockdown mode.
-  if (base::win::GetVersion() >= base::win::Version::WIN8)
+  // TODO(ananta)
+  // Look into whether this causes a loss of functionality. From cursory
+  // testing things seem to work well.
+  if (sandbox::policy::IsWin32kLockdownEnabled())
     return CreateImageDataSimple(instance, format, size, init_to_zero);
 #endif
   return PPB_ImageData_Impl::Create(instance,
diff --git a/sandbox/policy/switches.cc b/sandbox/policy/switches.cc
index b585cdfff3672..b7ce99202a894 100644
--- a/sandbox/policy/switches.cc
+++ b/sandbox/policy/switches.cc
@@ -6,6 +6,7 @@
 
 #include "build/build_config.h"
 #include "base/win/windows_version.h"
+#include "base/command_line.h"
 
 namespace sandbox::policy::switches {
 
@@ -70,6 +70,9 @@ const char kDisableSeccompFilterSandbox[] = "disable-seccomp-filter-sandbox";
 // Disable the setuid sandbox (Linux only).
 const char kDisableSetuidSandbox[] = "disable-setuid-sandbox";
 
+// Disables the Win32K process mitigation policy for child processes.
+const char kDisableWin32kLockDown[] = "disable-win32k-lockdown";
+
 // Allows shmat() system call in the GPU sandbox.
 const char kGpuSandboxAllowSysVShm[] = "gpu-sandbox-allow-sysv-shm";
 
@@ -123,3 +126,18 @@ const char kCloudPrintServiceProcess[] = "service";
 const char kCodeSignCloneCleanupProcessType[] = "code-sign-clone-cleanup";
 
 }  // namespace sandbox::policy::switches
+
+#if BUILDFLAG(IS_WIN)
+
+namespace sandbox {
+namespace policy {
+bool IsWin32kLockdownEnabled() {
+  return base::win::GetVersion() >= base::win::Version::WIN8 &&
+         !base::CommandLine::ForCurrentProcess()->HasSwitch(
+             switches::kDisableWin32kLockDown);
+}
+}  // namespace policy
+}  // namespace sandbox
+
+#endif
+
diff --git a/sandbox/policy/switches.h b/sandbox/policy/switches.h
index 1c6df38f5063c..ae0d64bf1d23e 100644
--- a/sandbox/policy/switches.h
+++ b/sandbox/policy/switches.h
@@ -50,6 +50,7 @@ SANDBOX_POLICY_EXPORT extern const char kDisableGpuSandbox[];
 SANDBOX_POLICY_EXPORT extern const char kDisableNamespaceSandbox[];
 SANDBOX_POLICY_EXPORT extern const char kDisableSeccompFilterSandbox[];
 SANDBOX_POLICY_EXPORT extern const char kDisableSetuidSandbox[];
+SANDBOX_POLICY_EXPORT extern const char kDisableWin32kLockDown[];
 SANDBOX_POLICY_EXPORT extern const char kGpuSandboxAllowSysVShm[];
 SANDBOX_POLICY_EXPORT extern const char kGpuSandboxFailuresFatal[];
 SANDBOX_POLICY_EXPORT extern const char kNoSandbox[];
@@ -79,5 +80,16 @@ SANDBOX_POLICY_EXPORT extern const char kCloudPrintServiceProcess[];
 SANDBOX_POLICY_EXPORT extern const char kCodeSignCloneCleanupProcessType[];
 
 }  // namespace sandbox::policy::switches
+
+namespace sandbox {
+namespace policy {
+#if BUILDFLAG(IS_WIN)
+// Returns whether Win32k lockdown is enabled for child processes or not.
+// Not really a switch, but uses one under the covers.
+SANDBOX_POLICY_EXPORT bool IsWin32kLockdownEnabled();
+#endif
+}  // namespace policy
+}  // namespace sandbox
+
 
 #endif  // SANDBOX_POLICY_SWITCHES_H_
diff --git a/sandbox/policy/win/sandbox_win.cc b/sandbox/policy/win/sandbox_win.cc
index 9b613183da9f8..2d40cdb1c1253 100644
--- a/sandbox/policy/win/sandbox_win.cc
+++ b/sandbox/policy/win/sandbox_win.cc
@@ -748,8 +748,7 @@ ResultCode SandboxWin::AddAppContainerPolicy(TargetPolicy* policy,
 // static
 ResultCode SandboxWin::AddWin32kLockdownPolicy(TargetConfig* config) {
   DCHECK(!config->IsConfigured());
-  // Win32k Lockdown is supported on Windows 8+.
-  if (base::win::GetVersion() < base::win::Version::WIN8)
+  if (!IsWin32kLockdownEnabled())
     return SBOX_ALL_OK;
 
   MitigationFlags flags = config->GetProcessMitigations();
@@ -933,7 +932,10 @@ ResultCode SandboxWin::StartSandboxedProcess(
     // TODO(crbug.com/40088338) Remove if we can reliably not load
     // cryptbase.dll.
     config->AddKernelObjectToClose(HandleToClose::kKsecDD);
+#if !defined(NACL_WIN64)
+  if (IsWin32kLockdownEnabled())
    result = SandboxWin::AddWin32kLockdownPolicy(config);
+#endif
    if (result != SBOX_ALL_OK) {
      return result;
    }
